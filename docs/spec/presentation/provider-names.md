# User-Facing Provider Names

The canonical user-facing provider names are `Codex`, `Claude`, and `Cursor`.

Each surface may apply its own typographic casing or surrounding format, such as an uppercase terminal header. Source identifiers and implementation identifiers are not provider names.
